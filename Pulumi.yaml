name: k8s-cluster-own
runtime: go
description: A eks cluster implementation completely integrated with aws (eg. volumes, ingress, scaling...). The idea is to have up and running cluster to learn build workflows in top of kubernetes.
